# Probably (hopefully?) not relevant to you. Change to suit your own needs.
# These are short form names, which I've defined in ~/.ssh/config.
role :boxen, "og", "aviary"

desc "Update the emacs.d repository"
task :deploy do
  run "cd ~/.emacs.d; rake update"
end
